export default function createComponent(type, content) {
    const element = document.createElement(type);
    element.textContent = content;
    return element;
}
//# sourceMappingURL=createComponent.js.map